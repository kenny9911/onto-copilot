# Secrets & local env — NEVER commit
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Runtime data & generated outputs (not source)
workspace/
out/
artifacts/
*.db
*.sqlite
*.sqlite3

# OS cruft
.DS_Store

# TS 迁移：依赖与构建产物不进版本库。
# 不写死 ts/ 前缀 —— 在仓库根误跑一次 npx 就会在根上刨出一个 node_modules，
# 而它一旦被 add 进来就是十万个文件的提交（这事已经发生过一次）。
node_modules/
dist/
